# GENERATED by tools/jumpstart/worlds/derive.py -- do not edit.
# Server-config overrides for world Ulfsland, preset pre-eikthyr (tier 1).
# Inputs:
#   tools/jumpstart/presets/pre-eikthyr.yaml   (tier definition, world-agnostic)
#   tools/jumpstart/worlds/tier-policy.yaml      (tier -> settings policy, world-agnostic)
#   tools/jumpstart/worlds/Ulfsland/world.yaml   (this world's facts)
# Regenerate:  tools/jumpstart/worlds/derive.py build Ulfsland
#
# Each entry is a SINGLE key in a SINGLE section of one deployed config file,
# with the value currently deployed alongside the value this tier wants. Whole
# config files are deliberately not copied here: nine copies of a 700-line file
# is the drift trap this layout exists to avoid.
#
# Deployed files live under profiles/ulfsland-dn/server-config .
# Applying these is an operator action; derive.py never writes a profile.
world: Ulfsland
preset: pre-eikthyr
tier: 1
server_config_root: profiles/ulfsland-dn/server-config
overrides:
- id: zenraids-raidchance
  config: ZenDragon.ZenRaids.cfg
  section: Raids General
  key: Raid Chance
  base: '-1'
  override: '0'
  evidence: MEASURED -- deployed value and semantics from the key's own comment block in ZenDragon.ZenRaids.cfg.
  why: 'Vanilla gates raids behind "the player has built something" -- `Raid Threshold - Limit
    = 3` base pieces. A jumpstart deletes that grace period, because BlueprintHunt''s placement
    drops a finished base at the spawn before the first login. At tier 2 the world already
    carries `defeated_eikthyr`, `Append Global Keys To Player Keys = true` pushes it onto
    every player, and `No Raid Biomes Exeption Raids = army_eikthyr` lets that raid into Meadows
    specifically -- so a tier-2 character can be raided in its home biome on day zero with
    no notice. Tiers 3+ are left at vanilla odds: by then the key set genuinely matches the
    kit and raids are the content, not an ambush.'
