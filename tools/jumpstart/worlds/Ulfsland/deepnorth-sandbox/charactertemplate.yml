# GENERATED by tools/jumpstart/worlds/derive.py -- do not edit.
# ServerCharacters CharacterTemplate.yml for world Ulfsland, preset deepnorth-sandbox (tier 9).
# Inputs:
#   tools/jumpstart/presets/deepnorth-sandbox.yaml   (tier definition, world-agnostic)
#   tools/jumpstart/worlds/tier-policy.yaml      (tier -> settings policy, world-agnostic)
#   tools/jumpstart/worlds/Ulfsland/world.yaml   (this world's facts)
# Regenerate:  tools/jumpstart/worlds/derive.py build Ulfsland
#
# REQUIRES ServerCharacters 1.4.17 or newer. 1.4.16 must NOT be
# installed: it hard-fails on Valheim 1.0.12 and ServerSync rejects it against a
# 1.4.17 server.
# STATUS: LIVE -- see world.yaml servercharacters.status_note.
#
# Install: copy this file to the world's GENERATED deploy layer,
#   Ulfsland/mods/generated/ServerCharacters/CharacterTemplate.yml
# then deploy. That layer is the fourth and last source the plugin tree is
# assembled from (cache -> manual-mods -> admin-mode -> generated), so it
# survives an admin-mode profile swap. It is deliberately NOT the profile's
# manual-mods overlay: manual-mods is PER PROFILE and this template is PER
# WORLD, so deploying a different profile left the file in no deploy source at
# all and the rebuild deleted it -- MEASURED tonight, and the operator met it as
# a new character with no kit and no preset spawn.
# The server watches the installed file and live-reloads; no restart. One server
# holds ONE template, so exactly one preset is active at a time.
#
# APPLIES TO NEW CHARACTERS ONLY. The client wipes inventory, skills, known
# recipes and stations, calls GiveDefaultItems(), then applies this. An existing
# character sees nothing.
#
# FIVE top-level keys are permitted by the build this world runs, and unknown
# keys are no longer fatal: the deserializer ignores unmatched properties and
# logs their names. The OLD 1.4.17 build is a different story -- it catches only
# SerializationException, which does not catch YamlDotNet's YamlException, so an
# unknown top-level key escapes a Harmony postfix and the WHOLE template is
# discarded: no skills, no items, no spawn. The DLL therefore has to reach the
# server and every client BEFORE this file is installed.
#
# What this format drops, and where it went instead:
#   overflow    54 prefabs (144 slots) do not fit in 32 inventory slots
#               or the 210 kg weight budget
#               and are in settings/chest-manifest.yaml instead.
#   stations    buildings, not inventory -- blueprints/ and placements.yaml.
#   keys        world state, not character state -- keys.yaml.
#   modifiers   launch arguments -- settings/world-modifiers.env.
#
# EQUIPPING. `equip:` is an ordered list, highest priority first, and every entry
# MUST also appear in `items:` because the mod can only equip what AddItem put in
# the inventory. Slot arbitration is the GAME's: the template names no slots,
# because m_itemType already decides which slot an item takes and a second source
# of truth could only ever disagree with the first. derive.py replays
# Humanoid::EquipItem over the list and REFUSES to render a template in which any
# listed item is displaced, so what is below is what the character wears:
#   helmet      HelmetDNHeavy
#   chest       ArmorDeepNorthHeavyChest
#   legs        ArmorDeepNorthHeavylegs
#   shoulder    BackpackMistlands
#   utility     Demister
#   trinket     TrinketBloodGoldHealth
#   right hand  MaceGold
#   left hand   ShieldGold
#   ammo        ArrowBloodGold
# Everything else stays in the bag, which is correct -- a player switches to the
# sledge when they want the sledge.
#
# QUALITY. Honoured now, and clamped at runtime to [1, m_shared.m_maxQuality] read
# live from the ObjectDB. 21 entries ask for more than 1. Quality does NOT
# change carried weight for any of them: MEASURED from ItemData::GetWeight, the
# quality term is gated on m_scaleWeightByQuality != 0, and across all 2,481 priced
# prefabs that field is nonzero for exactly 21 -- 20 fish variants at 1.0 and
# AsksvinEgg at 0.2. No weapon, shield, armour piece, tool or cape scales.
#
# CARRY LOAD: 95.4 kg of a MEASURED 300 kg capacity = 32%,
# leaving 204.6 kg of headroom against the 210 kg budget.
# Capacity is Player::m_maxCarryWeight (ldc.r4 300 in Player..ctor) times
# Game::m_carryWeightRate (1, no preset sets GlobalKey 0x10), with no status
# effect and nothing equipped on a fresh character. IsEncumbered is a STRICT
# greater-than, so the budget exists to leave room to pick things up, not to
# dodge a rounding error. Per-item weights: tools/jumpstart/data/item_weights.json.
# NOTHING EQUIPPED IS BUDGETED FOR, deliberately, and two levers are left on the
# table to keep it that way:
#   BeltStrength      +150 carry (MEASURED, SE_Stats.m_addMaxCarryWeight). It is
#                     Utility (m_itemType 18), the same slot as Demister and
#                     Wishbone, which five of the nine presets need; and no kit
#                     needs the 150 kg. Not granted anywhere.
#   AdventureBackpacks +5..30 carry per item level and a 0.5 multiplier on
#                     CONTENTS (MEASURED from the live, server-enforced
#                     vapok.mods.adventurebackpacks.cfg). The multiplier applies
#                     to nothing at spawn: AB keeps backpack contents in the
#                     item's m_customData and neither string overload of
#                     Inventory::AddItem takes custom data, so a template-granted
#                     backpack is necessarily EMPTY. It is granted and equipped as
#                     pure headroom, never as budget.
# The 95Shade-CarryWeightSkill +3/level bonus is likewise NOT budgeted for:
# MEASURED, its DLL reaches the client tree only and never the server, so it
# cannot be guaranteed for a player whose client lacks the package.
#
# Which of these skills actually move yield (MEASURED by YieldDesign). Five
# skills drive the gathering economy and three of them are custom skills
# registered by name through SkillManager, not members of the vanilla enum:
#   ore / stone   Smoothbrain `Mining` AND vanilla `Pickaxes` -- ImpactfulSkills'
#                 mining bonus reads vanilla Pickaxes (id 12). Both are needed.
#   wood          Smoothbrain `Lumberjacking` only. Vanilla `WoodCutting` is
#                 DELIBERATELY OMITTED below, not forgotten, and it is DOUBLY
#                 dead: Lumberjacking prefixes CheatRaiseSkill with
#                 RemoveWoodcuttingFromSkillFunctions, which rewrites the
#                 literal to "Woodcutting " so it matches nothing, and also
#                 prefixes GetSkill with ReturnDummyWoodcuttingSkill so any
#                 raise that got through lands on a throwaway object.
#                 ImpactfulSkills' WoodCuttingLootFactor = 3 is dead code.
#                 Do not add it back.
#   berries etc   Smoothbrain `Foraging` AND vanilla `Farming`.
#   crops         vanilla `Farming` only -- Smoothbrain Foraging's scope
#                 predicate needs Pickable.m_respawnTimeMinutes > 0 and crops are 0.
#   meat / hide   no skill at all; flat, at whatever the world resource rate is.
#
# Can this map REACH the custom skills? YES, all five, by plain name,
# case-insensitively. Established twice and independently: from the IL of
# assembly_valheim.dll and the deployed plugins, and from the live SkillManager
# registries and Harmony patch chain on a booted server. The two agree.
#   vanilla   Skills::.cctor sets s_allSkills = Enum.GetValues(SkillType) and
#             CheatRaiseSkill string-matches SkillType.ToString() against it, so
#             unaided it reaches enum members only. m_level += value, clamped
#             0..100 -- an ADD, so a set on a fresh character but it would
#             STACK if the template were applied twice.
#   custom    Each Smoothbrain plugin ILRepacks its own SkillManager, which
#             Harmony-PREFIXES CheatRaiseSkill with Patch_Skills_CheatRaiseskill.
#             It matches its registry's `internalSkillName` with
#             StringComparison.CurrentCultureIgnoreCase and returns false to
#             consume the call, else true and the chain continues. Three
#             independent prefixes, each matching only its own skill, so there
#             is no ordering hazard.
#   ids       Not enum members, so the prefix is the ONLY route: Mining =
#             1408976878, Foraging = 47719919, Lumberjacking = 1363793286,
#             each (SkillType)englishName.GetStableHashCode().
#   literal   internalSkillName = Regex.Replace(englishName, "[^a-zA-Z]", "_").
#             Registered english names are literally Mining / Foraging /
#             Lumberjacking -- pure alpha, so internal == display == what you
#             type. The exact casing below is the registered casing.
#   where     Applied CLIENT-side, in ServerCharacters.ClientSide
#             .InitializePlayerFromTemplate.Postfix, so the three Smoothbrain
#             mods must reach the client. They do -- all four are scope: shared
#             in the ulfsland-dn manifest.
#   trap      A matching prefix consumes the call, so a custom skill whose name
#             collides with a vanilla one captures it. Smoothbrain-Farming did
#             exactly that and was removed tonight; re-adding it would silently
#             change what the `Farming` line below means.
# `Farming` is RESOLVED: vanilla SkillType Farming is 0x6a = 106 in this build
# and there is no id 15 at all. ImpactfulSkills contains no SkillManager
# reference and reads GetSkillLevel(0x6a) -- vanilla Farming. Exactly one
# Farming exists and this line reaches it. ImpactfulSkills' OWN custom skills
# (Hauling, Voyager) are not template-settable; neither drives yield.

skills:
  Axes: 100.0
  Blacksmithing: 90.0
  Blocking: 100.0
  BloodMagic: 70.0
  Bows: 100.0
  Clubs: 95.0
  Cooking: 80.0
  Crafting: 100.0
  Crossbows: 90.0
  Dodge: 85.0
  ElementalMagic: 90.0
  Farming: 90.0
  Fishing: 50.0
  Foraging: 100.0
  Jump: 100.0
  Knives: 70.0
  Lumberjacking: 100.0
  Mining: 100.0
  Pickaxes: 100.0
  Polearms: 90.0
  Ride: 70.0
  Run: 100.0
  Sneak: 80.0
  Spears: 85.0
  Swim: 90.0
  Swords: 100.0
  Unarmed: 30.0
# 0 skills omitted at level 0: CheatRaiseSkill(name, 0) is a no-op, and an
# unresolvable name is logged and skipped, not fatal.
# WoodCutting omitted at 90 on purpose -- inert on this mod set,
# see world.yaml skills.omit. Re-adding it does nothing.
items:
  ArmorDeepNorthHeavyChest: 1
  ArmorDeepNorthHeavylegs: 1
  ArmorDeepNorthMageChest: 1
  ArmorDeepNorthMagelegs: 1
  ArmorDeepNorthMediumChest: 1
  ArmorDeepNorthMediumlegs: 1
  ArrowBloodGold: 50
  ArrowCharred: 50
  AtgeirGold: 1
  AxeGold: 1
  BackpackMistlands: 1
  BattleaxeGold: 1
  BoltBloodGold: 50
  BoltCharred: 50
  BowGold: 1
  CapeDeepNorth: 2
  CapeDeepNorthMage: 1
  CrossbowGold: 1
  Demister: 1
  FistGold: 1
  GrapplingHook: 2
  HelmetDNHeavy: 1
  HelmetDNMage: 1
  HelmetDNMediumHood: 1
  KnifeGold: 1
  Lantern: 1
  Lantern_DN: 1
  MaceGold: 1
  ShieldGold: 1
  TrinketBloodGoldHealth: 1
# 32 of 32 inventory slots used, 95.4 of 300 kg carried (32%), 204.6 kg free.
# Summed across buckets (the preset lists these twice, and `items` is a map): CapeDeepNorth, FrostCore, GrapplingHook, MeadFrostResist, SealHide
quality:
  ArmorDeepNorthHeavyChest: 4
  ArmorDeepNorthHeavylegs: 4
  ArmorDeepNorthMageChest: 4
  ArmorDeepNorthMagelegs: 4
  ArmorDeepNorthMediumChest: 4
  ArmorDeepNorthMediumlegs: 4
  AtgeirGold: 4
  AxeGold: 4
  BackpackMistlands: 4
  BattleaxeGold: 4
  BowGold: 4
  CapeDeepNorth: 4
  CapeDeepNorthMage: 4
  CrossbowGold: 4
  FistGold: 4
  HelmetDNHeavy: 4
  HelmetDNMage: 4
  HelmetDNMediumHood: 4
  KnifeGold: 4
  MaceGold: 4
  ShieldGold: 3
# 21 entries above the default 1; everything else is 1 by omission.
# 21 of them sit at the item's measured m_maxQuality: ArmorDeepNorthHeavyChest 4, ArmorDeepNorthHeavylegs 4, ArmorDeepNorthMageChest 4, ArmorDeepNorthMagelegs 4, ArmorDeepNorthMediumChest 4, ArmorDeepNorthMediumlegs 4, AtgeirGold 4, AxeGold 4, BackpackMistlands 4, BattleaxeGold 4, BowGold 4, CapeDeepNorth 4, CapeDeepNorthMage 4, CrossbowGold 4, FistGold 4, HelmetDNHeavy 4, HelmetDNMage 4, HelmetDNMediumHood 4, KnifeGold 4, MaceGold 4, ShieldGold 3.
equip:
  - HelmetDNHeavy
  - ArmorDeepNorthHeavyChest
  - ArmorDeepNorthHeavylegs
  - BackpackMistlands
  - MaceGold
  - ShieldGold
  - ArrowBloodGold
  - Demister
  - TrinketBloodGoldHealth
# 9 slots filled, replayed through the game's own arbitration:
#   helmet      HelmetDNHeavy (m_itemType Helmet, quality 4)
#   chest       ArmorDeepNorthHeavyChest (m_itemType Chest, quality 4)
#   legs        ArmorDeepNorthHeavylegs (m_itemType Legs, quality 4)
#   shoulder    BackpackMistlands (m_itemType Shoulder, quality 4)
#   utility     Demister (m_itemType Utility, quality 1)
#   trinket     TrinketBloodGoldHealth (m_itemType Trinket, quality 1)
#   right hand  MaceGold (m_itemType OneHandedWeapon, quality 4)
#   left hand   ShieldGold (m_itemType Shield, quality 3)
#   ammo        ArrowBloodGold (m_itemType Ammo, quality 1)
spawn:
  - {x: 768, y: 37, z: 1146}
  - {x: 700, y: 36, z: -55}
  - {x: -262, y: 39, z: 203}
  - {x: -262, y: 39, z: 203}
# Last entry is a deliberate duplicate: selection is Random.Range(0, count - 1),
# whose upper bound is exclusive, so without it the real last point is dead.
# MEASURED against Valheim's own generator at 1 m, water plane y=30:
#   (768, 37, 1146)  ground 35.49 = +5.49 m over water, 6.6 m outside complete-station-hub's footprint (31.1 m from its centre),
#     dry across a 4 m disc (min +4.31 m) and dry every metre of the 6.6 m walk to it.
#   (700, 36, -55)  ground 34.1 = +4.1 m over water, 13.9 m outside sandbox-harbour's footprint (39.3 m from its centre),
#     dry across a 4 m disc (min +2.72 m) and dry every metre of the 13.9 m walk to it.
#   (-262, 39, 203)  ground 37.09 = +7.09 m over water, 6.0 m outside sandbox-portal-hub's footprint (12.0 m from its centre),
#     dry across a 4 m disc (min +5.47 m) and dry every metre of the 6.0 m walk to it.
# Evidence: spawn.yaml. Re-verify: derive.py spawn Ulfsland
